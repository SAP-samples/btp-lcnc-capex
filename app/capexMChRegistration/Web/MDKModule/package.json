{
  "name": "MDKModule",
  "version": "0.0.1",
  "devDependencies": {
    "@sapui5/ts-types": "1.71.x",
    "@ui5/cli": "2.2.6",
    "@sap/ui5-builder-webide-extension": "1.0.x",
    "bestzip": "2.1.4",
    "rimraf": "3.0.2"
  },
  "scripts": {
    "build": "npm run clean && ui5 build --include-task=generateManifestBundle generateCachebusterInfo && npm run zip",
    "zip": "mdk amend --target cloud && cd dist && npx bestzip ../MDKModule-content.zip *",
    "clean": "npx rimraf MDKModule-content.zip dist",
    "buildmdk": "cd ../.. && mdk build --target module --ui5 && cd Web/MDKModule && npm run sync2mdk",
    "sync2mdk": "rm -rf ../../.dist && npm install && npm run build && mkdir ../../.dist && mv MDKModule-content.zip ../../.dist/capexMChRegistration.zip"
  },
  "ui5": {
    "dependencies": [
      "@sap/ui5-builder-webide-extension"
    ]
  }
}
